# Contributing

Publishing:

```bash
vsce publish
```

When asked to authenticate, make sure the publisher in `package.json` corresponds with the publisher name (may be different from Microsoft user name) you created on the account the API key is from.
